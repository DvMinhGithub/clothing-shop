<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="UserDtoResultMap" type="com.example.demo.model.dto.UserDto" autoMapping="true">
        <id column="id" property="id"/>
        <result column="address" property="address"/>
        <result column="avatar" property="avatar"/>
        <result column="dob" property="dob"/>
        <result column="email" property="email"/>
        <result column="gender" property="gender"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="phoneNumber" property="phoneNumber"/>
        <collection property="listRole" javaType="list" ofType="com.example.demo.enums.UserRole" autoMapping="true">
            <result column="roleName"/>
        </collection>
    </resultMap>

    <select id="getByEmail" resultMap="UserDtoResultMap">
        SELECT u.id as id,
        u.address as address,
        u.avatar as avatar,
        u.dob as dob,
        u.email as email,
        u.gender as gender,
        u.name as name,
        u.password as password,
        u.phone_number as phoneNumber,
        r.id as roleId,
        r.name as roleName
        FROM user u
        LEFT JOIN user_role ur ON u.id = ur.user_id
        LEFT JOIN role r ON ur.role_id = r.id
        WHERE u.email = #{email}
    </select>

    <insert id="register" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(address, dob, email, gender, name, password, phone_number)
        VALUES (#{userInfo.address},
        #{userInfo.dob},
        #{userInfo.email},
        #{userInfo.gender},
        #{userInfo.name},
        #{userInfo.password},
        #{userInfo.phoneNumber})
    </insert>

    <select id="existsByPhoneNumber" resultType="java.lang.Boolean">
        SELECT EXISTS(SELECT 1 FROM user u WHERE u.phone_number = #{phoneNumber})
    </select>

    <select id="existsByEmail" resultType="java.lang.Boolean">
        SELECT EXISTS(SELECT 1 FROM user u WHERE u.email = #{email})
    </select>

    <select id="getById" resultMap="UserDtoResultMap">
        SELECT u.id as id,
        u.address as address,
        u.avatar as avatar,
        u.dob as dob,
        u.email as email,
        u.gender as gender,
        u.name as name,
        u.password as password,
        u.phone_number as phoneNumber,
        r.name as roleName
        FROM user u
        LEFT JOIN user_role ur ON u.id = ur.user_id
        LEFT JOIN role r ON ur.role_id = r.id
        WHERE u.id = #{userId}
    </select>

    <delete id="deleteById">
        DELETE
        FROM user
        WHERE id = #{userId}
    </delete>

    <update id="updateProfile">
        UPDATE user
        SET name = #{userInfo.name},
        phone_number = #{userInfo.phoneNumber},
        address = #{userInfo.address},
        gender = #{userInfo.gender},
        dob = #{userInfo.dob},
        avatar = #{userInfo.avatar}
        WHERE id = #{userId}
    </update>

    <select id="findEmployeeByName" resultMap="UserDtoResultMap">
        SELECT u.id as id,
        u.address as address,
        u.avatar as avatar,
        u.dob as dob,
        u.email as email,
        u.gender as gender,
        u.name as name,
        u.password as password,
        u.phone_number as phoneNumber,
        r.name as roleName
        FROM user u
        INNER JOIN user_role ur ON u.id = ur.user_id
        INNER JOIN role r ON ur.role_id = r.id
        WHERE r.name = 'EMPLOYEE'
        AND u.name LIKE CONCAT("%", #{employeeName}, "%")
    </select>

    <select id="findEmployee" resultMap="UserDtoResultMap">
        SELECT u.id as id,
        u.address as address,
        u.avatar as avatar,
        u.dob as dob,
        u.email as email,
        u.gender as gender,
        u.name as name,
        u.password as password,
        u.phone_number as phoneNumber,
        r.name as roleName
        FROM user u
        INNER JOIN user_role ur ON u.id = ur.user_id
        INNER JOIN role r ON ur.role_id = r.id
        WHERE r.name = 'EMPLOYEE'
    </select>
</mapper>
